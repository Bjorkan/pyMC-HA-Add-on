# Home Assistant Add-on: pyMC Glass Main

Run `pyMC_Glass` inside Home Assistant with persistent database, PKI, MQTT, and
Glass runtime configuration.

This add-on tracks the upstream `pyMC-Glass` `main` branch.
Its configuration lives at `/config/config.yaml` inside the add-on config mount.
