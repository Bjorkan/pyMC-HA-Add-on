# Home Assistant Add-on: pyMC Repeater Main

Run `pyMC_Repeater` inside Home Assistant with a persistent configuration file
and persistent repeater data storage.

This add-on tracks the upstream `pymcdev/pymc-repeater:main` image.
Its configuration lives at `/config/config.yaml` inside the add-on config mount.
