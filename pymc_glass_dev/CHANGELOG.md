# Changelog

## 1.0.0

- Add Home Assistant wrapper for upstream `pyMC-Glass` `dev` branch
- Package Glass frontend, backend, PostgreSQL, and Mosquitto into one add-on
- Persist Glass config in `addon_config/*_pymc_glass_dev/config.yaml`
- Persist Glass database, PKI, and MQTT data in `/var/lib/pymc_glass`
